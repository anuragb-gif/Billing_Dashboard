node_modules/
dist/
backend/data/
backend/.env
*.log

# Vercel CLI link folder - never commit (holds project/org IDs)
.vercel/

# dev tunnel binary - downloaded per-machine, not source (see deploy/TUNNEL-RUNBOOK.md)
deploy/devtunnel.exe
